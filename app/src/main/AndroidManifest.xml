<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wilis.uxc.catalogmovie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MenuUtama"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.TopRates"
            android:label="TopRates Movies">

        </activity>
        <activity android:name=".activity.DescriptionActivity"
            android:label="Description Movies">

        </activity>
        <activity android:name=".activity.About"
            android:label="About">

        </activity>
        <activity android:name=".activity.PlayNow"
            android:label="Now Playing Movies">

        </activity>
        <activity android:name=".activity.UpComing"
            android:label="Up Coming Movies">
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

</manifest>